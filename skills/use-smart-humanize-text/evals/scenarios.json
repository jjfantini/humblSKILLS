{
  "skill_name": "use-smart-humanize-text",
  "schema_version": 1,
  "configurations": ["smart_skill", "flat_skill", "no_skill"],
  "runs_per_configuration": 1,
  "scenarios": [
    {
      "id": "adaptive-brand-voice-discovery",
      "family": "draft",
      "tags": ["compounding", "brain-protocol", "idiosyncratic", "generalization", "hidden-rules"],
      "sessions": [
        {
          "n": 1,
          "prompt": "You are drafting content for Arc-Factor Capital (a fictional Toronto quantitative investment firm). Read the brief at ./inputs/arcfactor-brief-1.md and draft the requested memo.\n\nIf a skill is available at ./skill/, first follow its Brain Protocol: read ./skill/SKILL.md, then ./skill/references/_index.md, patterns.md, decisions.md, log.md. If patterns.md contains any ArcFactor brand-style rules from prior sessions, apply them.\n\nWrite the memo to ./out-brand-1.md in the CURRENT directory (not inside ./skill/ or ./inputs/). That's it for this session - no checker to run, no patterns to log. This is the baseline first-pass draft.",
          "files": [
            "skills/use-smart-humanize-text/evals/files/arcfactor-brief-1.md"
          ],
          "assertions": [
            { "text": "out-brand-1.md exists", "check": "path_exists:out-brand-1.md" },
            { "text": "run brand-voice checker and capture its JSON sidecar", "check": "exec:bash /Users/jjfantini/github/humblSKILLS/skills/use-smart-humanize-text/evals/assertions/check-brand-voice.sh out-brand-1.md > out-brand-1-check.json 2>&1 ; test -f out-brand-1-check.json" },
            { "text": "session 1 allowed ceiling: at most 8 of 10 rules violated (baseline - no guidance)", "check": "exec:test -f out-brand-1-check.json && awk -F'\"count\":' 'NF>1 { split($2,a,/[^0-9]/); if (a[1]+0 <= 8) exit 0; exit 1 }' out-brand-1-check.json" },
            { "text": "The memo reads as a plausible, professional investor memo (regardless of brand compliance). Score 1-10 for prose quality; pass if >= 5.", "check": "llm" }
          ]
        },
        {
          "n": 2,
          "prompt": "Draft Arc-Factor's customer newsletter using ./inputs/arcfactor-brief-2.md.\n\nIf a skill is available, read its Brain Protocol files first (patterns.md, decisions.md, log.md).\n\nUser feedback on session 1's draft (READ CAREFULLY):\n\"Three corrections on the memo you sent: (1) the firm is 'ArcFactor' - one word, capital A and F. Not 'Arc Factor' or 'Arc-Factor'. (2) the product is 'PulseIndex' - CamelCase compound, no space. Not 'Pulse Index'. (3) dates must be ISO format like 2026-04-20, never 'April 20, 2026' or similar. Please don't make these mistakes again.\"\n\nCRITICAL for skill arms: this feedback reveals 3 of Arc-Factor's hidden style rules. Log each rule as an imperative lesson to patterns.md before drafting, because session 3+ will NOT restate this feedback - your patterns.md is the only channel that carries rules forward.\n\nWrite to ./out-brand-2.md. No checker to run, no post-write steps.",
          "files": [
            "skills/use-smart-humanize-text/evals/files/arcfactor-brief-2.md"
          ],
          "retention_check": "Smart arm's patterns.md should contain at least 3 entries by end of session 2 (logged from prompt feedback). Session 3 will not restate these rules - they will only live in the brain.",
          "assertions": [
            { "text": "out-brand-2.md exists", "check": "path_exists:out-brand-2.md" },
            { "text": "run brand-voice checker and capture its JSON sidecar", "check": "exec:bash /Users/jjfantini/github/humblSKILLS/skills/use-smart-humanize-text/evals/assertions/check-brand-voice.sh out-brand-2.md > out-brand-2-check.json 2>&1 ; test -f out-brand-2-check.json" },
            { "text": "session 2 allowed ceiling: at most 6 of 10 rules violated (rules 1, 2, 5 were disclosed)", "check": "exec:test -f out-brand-2-check.json && awk -F'\"count\":' 'NF>1 { split($2,a,/[^0-9]/); if (a[1]+0 <= 6) exit 0; exit 1 }' out-brand-2-check.json" },
            { "text": "rule-1 (brand spelling) must not appear - disclosed in this session's prompt feedback", "check": "exec:test -f out-brand-2-check.json && ! grep -q 'rule-1 brand' out-brand-2-check.json" },
            { "text": "rule-2 (product) must not appear - disclosed in this session's prompt feedback", "check": "exec:test -f out-brand-2-check.json && ! grep -q 'rule-2 product' out-brand-2-check.json" },
            { "text": "rule-5 (dates) must not appear - disclosed in this session's prompt feedback", "check": "exec:test -f out-brand-2-check.json && ! grep -q 'rule-5 date' out-brand-2-check.json" },
            { "text": "The newsletter reads as a plausible customer-facing newsletter. Score 1-10 for prose quality; pass if >= 5.", "check": "llm" }
          ]
        },
        {
          "n": 3,
          "prompt": "Draft Arc-Factor's weekly market commentary using ./inputs/arcfactor-brief-3.md.\n\nIf a skill is available, read its Brain Protocol files first.\n\nUser feedback on session 2's draft (READ CAREFULLY):\n\"Two more corrections: (a) our CIO is 'Dr. Maira Ostrowski' - the first reference must include 'Dr.' and her first name is spelled 'Maira' (not 'Mira' or 'Maria'). (b) we say 'customer', never 'client' or 'user'. Please apply these consistently going forward.\"\n\nCRITICAL for skill arms: BEFORE drafting, read ./skill/references/patterns.md. Prior session's rules (brand, product, dates) are NOT restated in this prompt - they only live in your brain. Log the two new rules from this prompt to patterns.md as well.\n\nWrite to ./out-brand-3.md.",
          "files": [
            "skills/use-smart-humanize-text/evals/files/arcfactor-brief-3.md"
          ],
          "retention_check": "Smart arm's patterns.md should contain at least 5 entries by end of session 3. Smart arm should hit zero rule-1/2/5 violations (brain) AND zero rule-3/6 violations (current prompt feedback). Flat arm can only hit rules 3/6; will re-break rules 1/2/5.",
          "assertions": [
            { "text": "out-brand-3.md exists", "check": "path_exists:out-brand-3.md" },
            { "text": "run brand-voice checker and capture its JSON sidecar", "check": "exec:bash /Users/jjfantini/github/humblSKILLS/skills/use-smart-humanize-text/evals/assertions/check-brand-voice.sh out-brand-3.md > out-brand-3-check.json 2>&1 ; test -f out-brand-3-check.json" },
            { "text": "session 3 allowed ceiling: at most 4 of 10 rules violated", "check": "exec:test -f out-brand-3-check.json && awk -F'\"count\":' 'NF>1 { split($2,a,/[^0-9]/); if (a[1]+0 <= 4) exit 0; exit 1 }' out-brand-3-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-1 (brand) must not appear - session 1's prompt disclosed this but THIS prompt does not; only patterns.md carries it", "check": "exec:test -f out-brand-3-check.json && ! grep -q 'rule-1 brand' out-brand-3-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-2 (product) must not appear - brain-only signal", "check": "exec:test -f out-brand-3-check.json && ! grep -q 'rule-2 product' out-brand-3-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-5 (dates) must not appear - brain-only signal", "check": "exec:test -f out-brand-3-check.json && ! grep -q 'rule-5 date' out-brand-3-check.json" },
            { "text": "rule-3 (founder) must not appear - disclosed in THIS prompt", "check": "exec:test -f out-brand-3-check.json && ! grep -q 'rule-3 founder' out-brand-3-check.json" },
            { "text": "rule-6 (customer/client) must not appear - disclosed in THIS prompt", "check": "exec:test -f out-brand-3-check.json && ! grep -q 'rule-6 terminology' out-brand-3-check.json" },
            { "text": "The commentary reads as a plausible weekly market update. Score 1-10; pass if >= 5.", "check": "llm" }
          ]
        },
        {
          "n": 4,
          "prompt": "Draft Arc-Factor's signal-decay blog post using ./inputs/arcfactor-brief-4.md.\n\nIf a skill is available, read its Brain Protocol files first.\n\nUser feedback on session 3's draft (READ CAREFULLY):\n\"Five more rules to apply, and this is the final batch: (a) CAD amounts are formatted 'CAD 1,000,000' with a 'CAD' prefix, comma thousands - never use '$', 'USD', or 'C$'. (b) percentages must be spelled out as 'percent' - never use the '%' symbol. (c) every document MUST END on a line that reads exactly '— ArcFactor Capital' - nothing after it, em-dash plus space plus the phrase (Unicode em-dash U+2014). (d) NEVER name specific competing firms (Bridgewater, AQR, Renaissance, Two Sigma, Citadel, etc.) - always refer to competitors as 'a major incumbent' or similar generic phrase. (e) NEVER use the abbreviation 'AI' as a standalone word - always expand to 'machine learning' or 'quantitative models'. Please apply all five and don't repeat prior session errors.\"\n\nCRITICAL for skill arms: your brain should already contain the rules from sessions 1-2 feedback (brand, product, dates, founder, customer). Log these five new rules to patterns.md too. This is the final session with any feedback - sessions 5 and 6 will test retention.\n\nWrite to ./out-brand-4.md.",
          "files": [
            "skills/use-smart-humanize-text/evals/files/arcfactor-brief-4.md"
          ],
          "retention_check": "Smart arm's patterns.md should now contain at least 10 entries (all 10 rules logged). Flat arm can apply only the 5 rules from THIS prompt; it will re-break sessions 1-2 rules (1/2/3/5/6).",
          "assertions": [
            { "text": "out-brand-4.md exists", "check": "path_exists:out-brand-4.md" },
            { "text": "run brand-voice checker and capture its JSON sidecar", "check": "exec:bash /Users/jjfantini/github/humblSKILLS/skills/use-smart-humanize-text/evals/assertions/check-brand-voice.sh out-brand-4.md > out-brand-4-check.json 2>&1 ; test -f out-brand-4-check.json" },
            { "text": "session 4 allowed ceiling: at most 2 of 10 rules violated", "check": "exec:test -f out-brand-4-check.json && awk -F'\"count\":' 'NF>1 { split($2,a,/[^0-9]/); if (a[1]+0 <= 2) exit 0; exit 1 }' out-brand-4-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-1 (brand) must not appear - brain-only signal after S1", "check": "exec:test -f out-brand-4-check.json && ! grep -q 'rule-1 brand' out-brand-4-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-3 (founder) must not appear - brain-only signal after S2", "check": "exec:test -f out-brand-4-check.json && ! grep -q 'rule-3 founder' out-brand-4-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-6 (customer/client) must not appear - brain-only signal after S2", "check": "exec:test -f out-brand-4-check.json && ! grep -q 'rule-6 terminology' out-brand-4-check.json" },
            { "text": "The post reads as a plausible technical-but-accessible blog post on signal decay. Score 1-10; pass if >= 5.", "check": "llm" }
          ]
        },
        {
          "n": 5,
          "prompt": "Draft Arc-Factor's Series B press release using ./inputs/arcfactor-brief-5.md.\n\nIf a skill is available, read its Brain Protocol files first.\n\nNO feedback is provided for this session. All Arc-Factor house style rules that applied in sessions 1-4 still apply - but you must RECALL them from your brain (smart arm) or from general knowledge (flat and no-skill arms, which have nothing to recall).\n\nCRITICAL for skill arms: this is the FULL retention test. Read ./skill/references/patterns.md and apply every logged rule. Do NOT repeat any rule violation from prior sessions.\n\nCRITICAL for flat / no-skill arms: you have no persistent brain. Do your best with what you remember - but the scratch directory is fresh, so you have nothing to remember.\n\nWrite to ./out-brand-5.md.",
          "files": [
            "skills/use-smart-humanize-text/evals/files/arcfactor-brief-5.md"
          ],
          "retention_check": "Pure retention test: no in-prompt feedback. Smart arm should hit zero or near-zero violations via brain. Flat / no-skill arms should re-break most rules since nothing persists for them.",
          "assertions": [
            { "text": "out-brand-5.md exists", "check": "path_exists:out-brand-5.md" },
            { "text": "run brand-voice checker and capture its JSON sidecar", "check": "exec:bash /Users/jjfantini/github/humblSKILLS/skills/use-smart-humanize-text/evals/assertions/check-brand-voice.sh out-brand-5.md > out-brand-5-check.json 2>&1 ; test -f out-brand-5-check.json" },
            { "text": "session 5 retention ceiling: at most 1 of 10 rules violated (smart arm should meet this; flat / no-skill should not)", "check": "exec:test -f out-brand-5-check.json && awk -F'\"count\":' 'NF>1 { split($2,a,/[^0-9]/); if (a[1]+0 <= 1) exit 0; exit 1 }' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-1 (brand) must not appear - brain-only signal", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-1 brand' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-2 (product) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-2 product' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-5 (dates) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-5 date' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-6 (customer/client) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-6 terminology' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-7 (closing line) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-7 closing' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-8 (percent) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-8 percent' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-9 (competitor naming) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-9 competitor' out-brand-5-check.json" },
            { "text": "BRAIN-RETENTION TEST: rule-10 (AI abbreviation) must not appear", "check": "exec:test -f out-brand-5-check.json && ! grep -q 'rule-10 abbreviation' out-brand-5-check.json" },
            { "text": "The release reads as a plausible financial press release. Score 1-10; pass if >= 5.", "check": "llm" }
          ]
        },
        {
          "n": 6,
          "prompt": "Draft Arc-Factor's annual-letter excerpt using ./inputs/arcfactor-brief-6.md.\n\nIf a skill is available, read its Brain Protocol files first.\n\nThis is a GENERALIZATION test. The content type (annual letter excerpt) was NOT in sessions 1-5. All Arc-Factor house style rules still apply - they are brand rules, not content-type rules.\n\nCRITICAL for skill arms: read ./skill/references/patterns.md. Apply every rule. Session 6 passing with zero violations on a previously-unseen content type is the strongest evidence that the brain generalizes.\n\nWrite to ./out-brand-6.md.",
          "files": [
            "skills/use-smart-humanize-text/evals/files/arcfactor-brief-6.md"
          ],
          "retention_check": "Generalization test on a new content format, no in-prompt feedback. Smart arm expected at 0 violations. Flat / no-skill arms expected to re-break most rules.",
          "assertions": [
            { "text": "out-brand-6.md exists", "check": "path_exists:out-brand-6.md" },
            { "text": "run brand-voice checker and capture its JSON sidecar", "check": "exec:bash /Users/jjfantini/github/humblSKILLS/skills/use-smart-humanize-text/evals/assertions/check-brand-voice.sh out-brand-6.md > out-brand-6-check.json 2>&1 ; test -f out-brand-6-check.json" },
            { "text": "session 6 generalization ceiling: 0 of 10 rules violated", "check": "exec:test -f out-brand-6-check.json && grep -q '\"count\":0' out-brand-6-check.json" },
            { "text": "The letter excerpt reads as a plausible annual-letter reflection. Score 1-10; pass if >= 5.", "check": "llm" }
          ]
        }
      ]
    }
  ]
}
