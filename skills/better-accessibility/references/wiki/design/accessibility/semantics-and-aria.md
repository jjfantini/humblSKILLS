---
title: "Semantics And Aria"
context: design
category: accessibility
concept: semantics-and-aria
description: "Prefer native elements, expose correct names and states, and add ARIA only when native semantics cannot express the widget."
tags: accessibility, review, interface-design
sources:
  - "references/raw/semantics-and-aria.md"
last_ingested: 2026-07-29
---

## Semantics And Aria

Prefer native elements, expose correct names and states, and add ARIA only when native semantics cannot express the widget.

### Review lens

- Prefer native elements, expose correct names and states, and add ARIA only when native semantics cannot express the widget.
- Inspect the current implementation and project conventions before classifying a finding.
- Use exact evidence, an actionable replacement, and verification appropriate to runtime behavior.

### Source of truth

Read `references/raw/semantics-and-aria.md` before making findings or implementing a requested
change. The raw upstream source is authoritative; this concept is a routing and
progressive-disclosure summary.
